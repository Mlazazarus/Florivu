#!/usr/bin/env bash
set -e

cat <<'EOF'
PlantDex is now a browser-first Vite app.

The old Expo/mobile scaffold generator has been intentionally removed so it does not overwrite the web refactor.

Use these commands from this folder instead:
  npm install
  npm run dev
  npm run build
EOF
